# JobOS DESIGN.md — 视觉与交互真相（唯一真源）

> 版本 v0.2 · 2026-09-15 · 状态：草案待本人最终拍板
> 本文件是 JobOS 控制台（`apps/web`）唯一视觉真源。任何页面/组件实现以本文件为准；参考产品模式见 [docs/design/references/](docs/design/references/README.md)；页面地图与业务口径见 [01 篇](docs/architecture/01-系统设计文档.md)；AI 编码行为边界见 [AGENTS.md](AGENTS.md)。
>
> 代理（人/AI）开发任意前端任务前检索链：**架构索引 → 本文件 →（按需）[参考库](docs/design/references/README.md) → taste skill（anti-slop 检查）→ AGENTS.md 红线**。
> v0.1 草案（[docs/design/frontend-design-draft-v0.1.md](docs/design/frontend-design-draft-v0.1.md)）已被本版替代，保留作演进留痕。

---

## 0. 合成地图（四份参考各取一面，禁止照抄任一）

| 参考 | 取其模式 | 明确不取 |
|---|---|---|
| [Linear](docs/design/references/linear.md) | 发丝线表面阶梯（不靠阴影）、单一稀缺强调色、紧凑几何（8px 按钮/12px 卡）、状态行、等宽状态文本、键盘优先 | 暗色营销画布、薰衣草蓝品牌色、负字距大标题 |
| [Notion](docs/design/references/notion.md) | 矩形而非胶囊按钮、软标签 chip（分类/规则标签）、发丝线表格行、文档式表面、行内编辑/斜杠菜单语感 | 紫色主按钮、海军蓝 hero、粉蜡色块、营销级深阴影 |
| [Claude](docs/design/references/claude.md) | 人与 AI 的视觉阶层（草稿虚线 vs 确认实线）、强调色稀有使用、深色表面只留给「系统铬」（raw/proto 查看器） | 奶油底+陶土色、衬线大标题、文学化排版 |
| [Vercel](docs/design/references/vercel.md) | 近白画布、墨色主操作、等宽眉标/技术标签、微堆叠阴影+内描边、状态语义色软底、数据表头规范 | 100px 胶囊营销 CTA、网格渐变、极性翻转黑色区段 |

### taste-skill 三个旋钮（本项目固定值）

```text
DESIGN_VARIANCE = 3/10   # 工具型产品：克制、栅格稳定，仅 signature 处允许变化
MOTION_INTENSITY = 2/10  # 仅状态反馈 120–160ms；无滚动叙事、无磁吸、无入场编排
VISUAL_DENSITY = 8/10   # 高频数据页默认高密度；长文阅读区切换宽松行高
```

taste skill（`.trae/skills/design-taste-frontend`）用于每次实现后的 **pre-flight anti-slop 检查**；其 landing-page 专属规则（hero、pricing 等）对本项目不适用。

## 1. Visual Theme & Atmosphere（主题与气质）

**一句话：本地案头上的求职调度终端——安静、精确、可扫读；颜色只在「需要你行动」与「证据状态」上出现。**

- 冷调近白工作画布，不暖不花；层级靠**底色阶梯 + 1px 发丝线**建立，几乎不靠阴影。
- 主操作是**墨色**（Vercel 式），不是彩色按钮海；彩色是稀有信号：朱=对外动作/风控，绿=证据/已确认，靛=AI/链接/焦点，琥珀=草稿/待定。
- 两个 signature（本产品的记忆点）：
  1. **漏斗导航**：左栏即七级业务管线，挂等宽计数与朱色待办点，顺序携带信息；
  2. **证据点 ●◐○**：贯穿 Claim/JD 标签/评分依据/Guard 的三态符号（实心/半圆/空心），同时用形状与颜色区分，色弱可辨。
- 深色表面只出现在「系统铬」：proto/raw JSON 查看器、终端类信息、缩略图占位；不做暗色营销区，不做全屏 dark theme（v1 仅浅色）。
- 无渐变 hero、无插画、无 emoji 当图标、无三张等大特性卡——这些是 AI slop，本产品零容忍。

## 2. Color Palette & Roles（色板与语义角色）

### 2.1 表面与墨色

| Token | hex | 角色 |
|---|---|---|
| `--canvas` | #F7F8FA | 全局底色（冷近白） |
| `--canvas-inset` | #F1F3F6 | 内嵌区：表格底、禁用块、raw 查看器外围 |
| `--sheet` | #FFFFFF | 卡片/纸面/弹层 |
| `--hairline` | #E3E6EB | 1px 默认描边/分隔线 |
| `--hairline-strong` | #C9CFD8 | 输入框边、更强分隔 |
| `--ink` | #1A1D21 | 标题/正文/主按钮面 |
| `--ink-2` | #4E5661 | 次要文字 |
| `--ink-3` | #8A919B | 弱化文字、占位、禁用 |
| `--console` | #16181C | 深色系统铬（raw/proto 查看器） |
| `--console-line` | #2A2E35 | 深色表面发丝线 |

### 2.2 信号色（严格按角色使用，不得挪作装饰）

| Token | hex | soft 底 | 允许出现的位置 |
|---|---|---|---|
| `--accent`（靛） | #2E5599 | #EAF1FA | AI 标记、链接、focus ring、运行中状态 |
| `--evidence`（松绿） | #2E7D5B | #E6F1EB | 证据充分、已确认、成功状态、confirmed |
| `--hold`（琥珀） | #94640A | #FAF1DC | AI 草稿、待确认、需人工注意（非错误） |
| `--seal`（印章朱） | #B23A2E | #F9E9E6 | **仅**：对外动作区、外发按钮描边、待人工拍板标记 |
| `--danger` | #B3261E | #F8E3E1 | 错误、风控暂停、破坏性操作 |
| `--info` | #3468B5 | #E9F0FB | 中性信息提示 |

规则：
- `--seal` 与 `--danger` 同属朱红家族：**seal 是"停，这一步只能你来"**（描边/标签/动作区边框），**danger 是"出错了"**（填底/报错）；同一屏两色不相邻大面积使用。
- **评分/匹配度禁止红绿配色**，分数永远与 Fit/Gap/Risk 文字同行；分数不是颜色。
- 任何信号色不得做整段背景或大色块；soft 底仅用于标签/提示条/行高亮。
- 白/浅模式唯一版本；深色仅 `--console` 局部表面。

### 2.3 数据可视化

- 漏斗：默认墨色灰阶（ink/ink-2/ink-3），**当前待处理级**用 `--accent`；转化为 0 的级不标红（0 不是错误）。
- 图表顺序色板（仅在多序列时）：#2E5599 · #2E7D5B · #94640A · #6B5B95 · #8A6E54；不使用彩虹色。
- 薪资口径、rule_id 等枚举标签用中性灰底，不用信号色。

## 3. Typography Rules（字体规则）

### 3.1 字体族

- **无衬线（UI/正文）**：`"Inter", "PingFang SC", "Noto Sans SC", "Microsoft YaHei", system-ui, sans-serif`
  （Inter 承担拉丁字符的工程产品感；中文走系统黑体，不打包中文字体）
- **等宽（技术层）**：`"JetBrains Mono", "IBM Plex Mono", ui-monospace, "SF Mono", Consolas, monospace`
  只用于：计数、ID/ext_id、时间戳、毫秒/节流值、rule_id、模型名、proto/JSON、表格眉标。
- 不引入衬线/展示字体；标题只靠字重（600/500）与灰度分级。中文正文不做负字距；拉丁大标题可用 -0.01em。

### 3.2 字阶（产品密度，不是营销字号）

| Token | 尺寸/行高/字重 | 用途 |
|---|---|---|
| title-lg | 20 / 28 / 600 | 页面标题（少数） |
| title | 16 / 24 / 600 | 卡片/区块标题 |
| body-emph | 14 / 22 / 500 | 表格主单元格、强调正文 |
| body | 14 / 22 / 400 | 默认正文/控件 |
| body-sm | 13 / 20 / 400 | 次要信息、密集表格 |
| caption | 12 / 18 / 400 | 眉标、标签内文、时间戳 |
| mono-sm | 12.5 / 20 / 400 | 技术数据 |
| reading | 15 / 28 / 400 | JD 原文、聊天等长文（最大栏宽 68ch） |
| btn | 13.5 / 20 / 500 | 按钮 |

数字一律等宽 + `font-variant-numeric: tabular-nums`（即使非 mono 字体的数字也启用表格数字）。

## 4. Component Stylings（组件规范）

### 4.1 按钮（矩形，四级 + 一个特殊区）

| 组件 | 样式 | 用途 |
|---|---|---|
| btn-primary | `--ink` 底白字，6px 圆角，高 32（紧凑 28） | 屏幕主操作，**每屏至多一个** |
| btn-secondary | 白底，1px `--hairline-strong`，ink 字 | 次级操作 |
| btn-ghost | 无底，hover 出浅底 | 工具栏/行内操作 |
| btn-danger | 白底 danger 字 + 1px danger/30% 描边；破坏性确认后才允许 danger 填底 | 清除/拒绝/中止 |
| **outreach-action** | 白字 seal 描边标签「对外动作」+ 白底 seal 文字按钮 | 仅限 [OutreachActionBar] 内：复制招呼语/填充预览/标记已发送 |

- 全部 6px 圆角矩形，**禁止 100px 胶囊**；仅状态徽标允许 pill。
- 禁用：disabled 用 `--canvas-inset` 底 + `--ink-3` 字（Guard 阻断时导出按钮即此态，旁边必须说明缺什么）。
- 加载中：按钮内 spinner + 禁用，不允许点击后无反馈。
- **界面中物理上不存在「一键发送/自动投递/自动翻页/自动上传」控件**（协议与 UI 双红线）。

### 4.2 卡片：AI 草稿态 vs 已确认态（全站统一语言）

| 组件 | 样式 | 语义 |
|---|---|---|
| card-sheet | 白底，1px `--hairline`，8px 圆角，内边距 16 | 普通内容 |
| card-draft | 白底，**1px 虚线 `--hold`**，左上角小标签「◐ AI 草稿 · 模型名 · 时间」 | 所有未经本人确认的 AI 产出 |
| card-confirmed | 白底，1px hairline，标签「● 已确认 · 时间」（evidence 绿点） | 本人确认后，方可进事实/导出/投影 |
| card-console | `--console` 深底，等宽浅字 | raw JSON/proto/解码原文等系统铬 |
| card-danger-zone | 白底 1px danger/40%，顶部 danger soft 条 | 风控/清除等危险区 |

### 4.3 证据点 EvidenceDot（signature 组件）

`●` evidence 实心绿（有证据）／`◐` hold 半圆琥珀（部分证据/需核）／`○` ink-3 空心（无证据，不可发布）。
悬停浮出证据卡（出处、原文句、来源类型）；出现在每条 Claim、JD Requirement 标签、评分依据、Guard 项之前。色觉之外形状独立承载信息。

### 4.4 表格（高频主组件）

- 行高 36（紧凑模式 30）；行底 1px `--hairline`；表头 caption + 等宽眉标，字色 `--ink-2`，不做整块重底色。
- 数字列右对齐+表格数字；状态列左对齐放 pill；hover 行 `--canvas-inset` 浅底；选中行 accent soft 底 + 左侧 2px accent 竖条。
- 淘汰行不删数据：文字降为 ink-3 + 行尾「捞回」ghost 按钮（捞回必须填理由）。
- 空表：EmptyState（一句话说明 + 一个主动作链接），不画插画。

### 4.5 标签与状态

- tag：6px 圆角矩形软底（neutral / accent soft / hold soft / evidence soft），12px；规则命中标签用 mono-sm（rule_id）。
- status-pill：pill 仅此处，soft 底 + 同色深字，前置状态点；running=accent 呼吸点（1.2s 透明度，仅此一处循环动画）。
- 模型/AI 标签：`◐ AI · gpt-xx` accent soft，不可省略——AI 产出必须可见地是 AI 产出。

### 4.6 输入与表单

- 高 32，6px 圆角，1px `--hairline-strong`；focus 为 2px accent/40% 外环（不靠改边框色 alone）。
- 错误：danger soft 底 + 具体修复说明（错误不道歉、不含糊）。
- **红线字段（公司名/职位名/起止时间/学历）在在线填充流中输入框置只读并标注「需你手填」**；载荷中出现这些字段时行内展示 skipped_redline 反馈。
- 节流/风控参数输入低于 1800ms、并发>1 即拒绝，拒绝说明引用红线而非仅说"非法输入"。

### 4.7 其他关键组件

| 组件 | 规范要点 |
|---|---|
| FunnelNav | 220px；七级竖排，级间 1px 竖线连接；计数 mono-sm 右置；有待办项时级名前置 seal 小点；当前页 accent 竖条+accent soft 底 |
| TopBar | 高 48，白底底发丝线；扩展连接灯（●灰/黄/绿）、节流只读快照、AI provider、上次备份；风控暂停时整条 danger soft + 深色停止图标 |
| OutreachActionBar | seal 1px 包围的条带：标题「对外动作（都由你本人完成，系统不会自动发送）」+ 三按钮 + 红线字段说明；全站唯一形状 |
| GuardList | 阻断（danger 点）/警告（hold 点）分组；阻断项置顶；存在阻断时关联主按钮 disabled |
| StatusTimeline | 只渲染 confirmed 事件；draft 不出现节点；新确认节点 150ms 淡入 |
| AiDraftCard 操作 | [确认] 右、[拒绝/修正] 左；危险分类（如标记拒绝）二次确认；附 DiffView 入口（AI 原文 vs 你的修订） |
| ClaimChip | 模板编辑器事实插槽；不可在模板内写死事实文本（写了保存即拒并定位） |
| Modal | 仅不可逆动作使用（清除聊天等），要求输入确认词；其余确认用行内/抽屉 |
| Toast | 与按钮同名（「标记已发送」→「已标记为已发送」）；3.2s；错误态常驻至关闭 |
| Banner | 隐私条（事件台常驻）：「聊天原文仅存本机、加密；不进入第三方请求」+ 按会话清除入口 |

## 5. Layout Principles（布局原则）

- 4px 基础栅格；token：4/8/12/16/24/32/48。卡片内边距 16，区块间距 24，页面边距 24。
- 桌面三栏（≥1280）：FunnelNav 220 / 主内容自适应 / ContextRail 320（可折叠，状态记忆）；TopBar 48。
- 数据页让表格吃满宽度，不设窄容器；阅读类（JD/聊天/复盘正文）最大 68ch；A4 编辑器纸张居中，工具靠边。
- 主流程动线与导航七级顺序一致；**候选区是唯一投递入口**，其他页面不出现进入投递包的按钮。
- 每屏一个主操作；右栏永远是当前对象的「第二视角」（证据/Guard/时间线三选一）。
- 留白服务扫读：同类信息用发丝线分组，不用卡片套卡片（最多两层表面嵌套，更深用留白）。

## 6. Depth & Elevation（深度）

以「表面阶梯 + 发丝线」为主，阴影保持 Vercel 式克制：

| 级 | 构成 | 用途 |
|---|---|---|
| 0 | 平面无描边 | 画布正文 |
| 1 | 1px hairline | 卡片、表格 |
| 2 | 内描边 `inset 0 0 0 1px #0000000F` | 次级抬升/hover |
| 3 | `0 1px 1px #00000006, 0 2px 4px #0000000A` + 内描边 | 抽屉、弹出层 |
| 4 | `0 2px 4px #0000000A, 0 12px 24px -8px #00000014` + 内描边 | 模态 |
| focus | 2px accent/40% 外环 | 键盘焦点（必备，2px 以上） |

禁止单层 8px+ 模糊大阴影；禁止阴影表达层级超过两级；深色 console 表面不用阴影用 `--console-line`。

## 7. Do's and Don'ts（含产品红线，前端部分）

### Do

- 先查页面地图（01 篇）与本文件再写组件；枚举/错误码从契约生成，不自造。
- 让每个数字可点开到来源（漏斗、计数、评分、证据数）；无来源不显示。
- AI 内容一律 card-draft 起手；确认后才换 card-confirmed 并允许下游使用。
- 用形状+文字承载状态（证据点、状态词），颜色只是加强。
- 报错给出下一步：缺证据说缺哪条、导入失败说 C 级原因、风控说命中什么词。
- 每个空状态给一个主动作；每个加载态给骨架或进度（采集批次给节流明细）。
- 所有交互键盘可达，Tab 顺序符合视觉顺序；`prefers-reduced-motion` 下禁用非必要动画。

### Don't（出现即缺陷/视为 slop）

1. 不实现/不放置：自动发送、一键投递、自动翻页、自动上传、绕过确认的开关；不给「加速采集」入口。
2. 不把 draft 投影成状态；不在确认前让 AI 内容进入导出/事实/时间线。
3. 不显示无来源分数、不用红绿表达匹配高低、不写「最佳匹配/保证录用/推荐投递」。
4. 不使用：渐变 hero、粉蜡色块、三色等大特性卡、emoji 图标、插画空状态、圆角胶囊主按钮、深色营销区。
5. 不在 UI 出现聊天原文于日志/错误上报/第三方请求预览；红线字段不做可写入控件。
6. 不让 showcase 模板出现 ATS 动作；3D 画廊默认关闭且不主导航占位。
7. 不在同一屏出现多个 ink 主按钮；不让彩色脱离语义角色。
8. 不手写 Zod 类型/枚举（契约单向生成）；不在前端硬编码业务文案事实（公司/职位/时间/学历）。

## 8. Responsive Behavior（响应式）

| 断点 | 行为 |
|---|---|
| ≥1280（主用） | 三栏完整；数据密度 8/10 |
| 1024–1279 | ContextRail 转右侧抽屉；表格保留列自定义 |
| 768–1023 | 左栏转图标+抽屉；表格横向滚动保留表头 |
| <768 | **只读模式**：漏斗/看板/详情可看；禁用编辑、填充、外发动作并说明「请在桌面端操作」 |

触控目标 ≥36px，移动断点 ≥44px；表格列在窄屏按「主标识+状态+主动作」折叠，不缩字号到 12 以下。

## 9. Agent Prompt Guide（代理使用指南）

### 9.1 开发任意页面前的固定开场

```text
读 DESIGN.md（本文件）+ 01 篇页面地图 + 该页契约 DTO；
声明：本页主操作是什么、AI 草稿元素有哪些、证据点出现在哪、有无对外动作；
然后再写代码。taste skill 旋钮：VARIANCE 3 / MOTION 2 / DENSITY 8。
```

### 9.2 页面→参考模式速查

| 要做的页面/模式 | 先翻参考 |
|---|---|
| 应用骨架、列表密度、状态行 | Linear §Components/Status、Vercel §Signature/data-table |
| 岗位池表格、规则标签、A4 编辑器斜杠菜单 | Notion §Tables/Badges/文档表面 |
| AiDraftCard、证据/溯源、聊天确认流 | Claude §Cards/Tags（只要阶层语言） |
| 批次/风控/配对/设置等开发者页 | Vercel §Components/Inputs/Status |
| 模板网格、缩略图卡 | Vercel template-card（矩形 16:9，不要画廊氛围） |

### 9.3 实现后 Pre-flight（提交前逐条过，红线条配负向测试由 05 测试篇承接）

1. 屏幕主操作是否唯一？墨色按钮有没有泛滥？
2. 每个 AI 元素是否带模型标签与草稿/确认态？找一个未确认元素验证它进不了下游。
3. 证据点三态是否形状可辨？每条 Claim 是否可溯源？
4. 本屏有没有对外动作？若有，是否只走 OutreachActionBar 且无直达站点操作？
5. 数字是否全部可下钻？枚举是否全部来自契约？
6. 把页面截成灰度：仅靠形状/文字，流程是否仍然成立？
7. 跑 slop 扫描：渐变 hero/等大卡三兄弟/胶囊 CTA/emoji 图标/粉蜡色，命中即返工。
8. 键盘走查一遍；窄屏只读策略是否生效。
9. 文案是否与按钮同名反馈？错误是否说明了怎么修？
10. 红线负向测试是否由实现同步补上（参考 [05 测试策略](docs/delivery/05-测试策略与验收.md)）。

### 9.4 变更规则

本文件是视觉真源：改 token/组件语义 = 设计变更，需本人评审后改本文件，再改代码；不允许在组件里先出现本文件没有的颜色/形状。新增页面模式先补本文件 §4/§9.2。
