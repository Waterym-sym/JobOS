# 06 项目状态快照

> 截至 2026-09-17 的代码与验证快照。它记录证据，不替代 [交付物清单](02-交付物清单.md)、[测试策略](05-测试策略与验收.md) 或独立人工评审；历史任务单的 `ready_for_human_review` 也不等于阶段通过。

## 当前结论

- 工作树基线为 `main` / `9e963b0`。提交标题含 v0.3.0，扩展包/manifest 为 0.3.0，但未见 Git 发布 tag，根包及 Python 包仍为 0.1.0；不能据此宣称 v0.3.0 已正式发布。
- 已有本地服务骨架、配对与 WS 网关、原始岗位采集/导入和补全队列、岗位池筛选入口、人工候选确认、控制台页面与 0001–0004 Alembic 迁移。扩展负责详情/公司补全；列表采集依赖只读外部 boss-helper，并非本仓库独立闭环。
- P1-001～P1-005 任务单均为 `ready_for_human_review`；P1-006 完成开发与隔离测试后仍须人审。未找到 M0/M1/P1 阶段出口的独立评审、真实小样本对账与发布记录。**当前不标记任何阶段门禁为已通过。**

## 阶段与里程碑

| 阶段 / 里程碑 | 可核对的实现 | 尚缺的出口证据或能力 | 结论 |
|---|---|---|---|
| P0 / M0 | 架构、机器契约、开发基线与任务单已建立 | 契约冻结与双端互跑的独立评审记录、Gold Set v0 完整证据 | 未验收 |
| P1 / M1 | Compose/服务骨架、配对/WS、岗位入池及补全、控制台、可逆 Alembic 迁移 | 全新机一次启动、旧 MySQL 对账、浏览器 E2E、≤30 条真实列表+详情样本与风控暂停演示、安全评审 | 部分实现，未验收 |
| P2 / M2 | 岗位池入口、人工候选确认与部分筛选流程 | Stage-0 八组规则、JD 解析、Stage-1 匹配/建议、真实漏斗对账与候选试用证据 | 早期切片，未验收 |
| P3 / M3 | 路由/占位页面 | 模板正交模型、导入/编辑、五格式导出与回读 | 未实现 |
| P4 / M4 | 路由/占位页面 | 三形态简历、Claim/Truth Guard、招呼语、投递包与人工预览填充 | 未实现 |
| P5 / M5 | 路由/占位页面 | 聊天回采、加密草稿、人工确认与状态投影 | 未实现 |
| P6 / M6 | 路由/占位页面 | 复盘、漏斗看板、策略生命周期、UAT/备份恢复 | 未实现 |

## 本次可复核验证

| 检查 | 结果 | 解释 |
|---|---|---|
| Python 3.12 默认 `pytest` | 161 passed、7 skipped、1 个 Starlette deprecation warning | 默认跳过需数据库/显式开关的测试；不是阶段验收 |
| 隔离 PostgreSQL 迁移往返 | 1 passed | 新建空 `jobos_migration_test` 库执行 `base → head → 0001 → base → head`；验证 0002 不删 0001 列及回滚后可重建。临时容器已停止 |
| `pnpm test` | 扩展 23 passed、Web 51 passed | 本机 Node 24 快速回归；项目规定 Node 20，不能替代锁定版本 CI |
| `pnpm typecheck`、`pnpm build` | 通过 | 同上，仍需 Node 20/CI 复核 |
| 全量 Ruff | 未通过 | 8 处现有测试文件风格问题，位于 `test_enrich_queue.py`、`test_extension_registry.py`、`test_screening_repo.py`；不改动业务范围外测试来伪造绿灯 |
| 本次迁移文件与往返测试 Ruff | 通过 | `ruff check migrations/versions tests/migration` |
| 全量 mypy | 未通过 | `services` 现有 4 个文件共 17 处类型错误；本次未改这些文件 |
| 浏览器 E2E / 真实账号小样本 | 无本次证据 | 不以单元测试推断真实性能或账号安全 |

## 关键风险与下一步

1. 先对 P1-001～P1-006 做独立人审，核对迁移逆操作的删数范围、备份/恢复方案及安全负例；不对用户库运行往返测试。
2. 清理 Ruff/mypy 存量问题，在锁定 Node 20 和 CI 环境重跑质量门禁；补浏览器合成页面 E2E、全新机启动与旧 MySQL 迁移对账。
3. 由本人按 [P1 出口门禁](02-交付物清单.md#p1-本地骨架--扩展重写) 进行真实小样本、风控暂停和入库幂等验收并留证，再决定 M1/P1 是否通过。
4. P2 按契约/DoR 拆 Stage-0、JD、匹配切片；现有筛选入口不得被当作规则引擎完成。

本快照不修改既有验收阈值、ADR、红线或历史决策，也不授权自动对外动作。
