# Domain modules

Future bounded contexts: `capture`, `screening`, `job`, `candidate`, `evidence`, `matching`, `resume`, `template`, `application`, and `retrospective`.

Each domain must expose an explicit public port. Domain internals and writable repositories must not cross this boundary.
