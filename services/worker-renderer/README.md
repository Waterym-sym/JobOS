# Worker renderer

This isolated process boundary will own document parsing and rendering. Chromium, WeasyPrint, `python-docx`, and CJK font assets are intentionally deferred until the rendering work package and license review.
